# Privacy And Security

- Keep all secrets in `.env.local`, Vercel env vars, or GitHub secrets.
- Never commit local student spaces or raw task sources.
- Public testimonials and results require both `published=true` and
  `consent_status=granted`.
- Student dashboards must not reveal teacher-only answers before permission.
- Auth is invite-only; public sign-up does not grant dashboard access.
